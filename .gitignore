# Runtime state (per-machine, not synced)
*/last_run.txt
twitter-pipeline/user_cache.json

# Tool config (local to each machine)
.mcp*.json
.claude/

# Python
__pycache__/
*.pyc

# macOS
**/.DS_Store

# Agent-Reach (large cloned repo, not our code)
twitter-pipeline/Agent-Reach/
